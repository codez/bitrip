<td id="remove_navi_link" class="right" width="100%">
<%= if index >= 0
	link_to_remote '<span>Remove the last navigation action</span> [-]', 
				   :url => { :controller => :navi_action, 
							 :action => :remove, 
							 :index => index }, 
				   :html => { :class => 'help' }
	end
%>
</td>