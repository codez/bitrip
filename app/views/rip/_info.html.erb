<div id="ripinfo">
<% if @rip -%>	
	<h3>
		<%= h @rip.description %> 
		<%= link_to '[..] <span style="font-size: smaller;">Edit this bitRip</span>', 
					id_action(:edit, @rip), 
					:class => 'help' %>
	</h3>

	<%= link_to "direct link", 
				:only_path => false, 
				:controller => @rip.name %>
	<%= link_to("[..] <span>Change parameters of this bitRip</span>", 
				id_action(:query, @rip), 
				:target => 'preview', 
				:class => 'help' ) if @rip.has_fields? %>
	<br/>
	<div class="small">
	Data by <%= @rip.sources.join(', ') %>
	</div>
	<div class="small" style="float: right;">
	<%= link_to "[+] <span style=>Clone this bitRip</span>", 
				id_action(:copy, @rip),
				:class => 'help' %>	
	<%= link_to "[-] <span>Delete this bitRip</span>", 
				{:action => :delete, :id => @rip.id},
				:method => :post,
				:confirm => 'Do you want to move this rip to the trash?',
				:class => 'help' %>	
	</div>
	<div class="small">
	Revision #<%= @rip.revision %> 
	<%= link_to "[history]", :action => :history, :id => @rip.name %> 
	</div>
	<br/>
<% end -%>
</div>